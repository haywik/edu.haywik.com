import subprocess


subprocess.run("python -m pip install platform",shell=True)

subprocess.run("python -m pip install importlib",shell=True)
subprocess.run("python -m pip install socket",shell=True)

input("Install finished")
